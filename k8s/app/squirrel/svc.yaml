apiVersion: v1
kind: Service
metadata:
  name: squirrel-core
  namespace: game
spec:
  type: ClusterIP
  selector:
    app: squirrel-core
  ports:
    - name: http
      port: 80
      targetPort: 9221
      protocol: TCP
