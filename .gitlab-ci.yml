stages:
  - smoke-test

variables:
  IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA

smoke-test:
  stage: smoke-test
  script:
    - echo "Runner is OK"
    - echo ${CI_COMMIT_REF_NAME}
    - echo ${CI_PIPELINE_SOURCE}
    - date
    - uname -a
