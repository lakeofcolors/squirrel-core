stages:
  - smoke-test
  - docker-test

variables:
  IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA

smoke-test:
  script: echo "Runner is OK"

docker-test:
  image: docker:stable
  services:
    - docker:dind
  script:
    - docker --version
    - docker run --rm hello-world
